# Agent Instructions

## Git & Authentication Guidelines
- **No Feature Branches:** NEVER create new branches or feature branches. Work, commit, and push directly to the `main` branch.
- **Commit Authorship:** Ensure all commits are authored by the `rpochtman-lang` GitHub account. 
  - The local repository git config is already configured with:
    - `user.name "rpochtman-lang"`
    - `user.email "269783741+rpochtman-lang@users.noreply.github.com"`
  - Do NOT modify or override these local settings.
- **Git Push:** Push directly to `origin main`. The remote `origin` is pre-authenticated with the token for `rpochtman-lang`.

## Build & Test Commands
- **Dev Server:** `npm run dev`
- **Build App:** `npm run build`
- **Linting:** `npm run lint`

---
@AGENTS.md
